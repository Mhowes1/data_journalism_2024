---
title: "data_project.Rmd"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
library(lubridate)
library(refinr)
library(janitor)
library(dplyr)
```

Rep. Jack Bergman (R-Mich.) was the program’s top spender last year. He was reimbursed more than $32,000 for lodging and nearly $12,000 for meals in 2023, according to data released by the House as of Tuesday.


```{r}
reimbursement_expenses <- read.csv("congress_data.csv")
```


```{r}
top_reimbursement_expenses <- reimbursement_expenses |>
  filter(FISCAL.YEAR.OR.LEGISLATIVE.YEAR == "LY2023") |>
  group_by(ORGANIZATION) |>
  summarise(
    AMOUNT = sum(AMOUNT)
  ) |>
  arrange(desc(AMOUNT))
```


